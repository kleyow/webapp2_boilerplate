#organization-fee-tab.tab-pane
  %h4 Processing Fees
    (#{"%.2f"|format(fee_percentage * 100)}%)

  %dl
    %dt USD
    %dd $#{"%.2f"|format(usd_fee / 100)}

    %dt JMD
    %dd $#{"%.2f"|format(jmd_fee / 100)}
